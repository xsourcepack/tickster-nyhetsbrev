import streamlit as st
import pandas as pd
import requests
import sqlite3
from datetime import datetime
import json
import os

st.set_page_config(
    page_title="Playa News",
    page_icon="🌴",
    layout="wide",
    initial_sidebar_state="expanded"
)

st.title("🌴 Playa News")
st.subheader("Tickster → Nyhetsbrev Dashboard")
st.caption("Automatisk synk från Tickster till Brevo | Skapa veckobrev enkelt")

# ====================== KONFIG ======================
TICKSTER_API_KEY = st.secrets.get("TICKSTER_API_KEY") or os.getenv("TICKSTER_API_KEY")
TICKSTER_BASE_URL = st.secrets.get("TICKSTER_BASE_URL") or os.getenv("TICKSTER_BASE_URL")
BREVO_API_KEY = st.secrets.get("BREVO_API_KEY") or os.getenv("BREVO_API_KEY")
BREVO_LIST_ID = st.secrets.get("BREVO_LIST_ID") or os.getenv("BREVO_LIST_ID")

# SQLite databas
conn = sqlite3.connect("kontakter.db", check_same_thread=False)

# Skapa tabell om den inte finns
conn.execute("""
    CREATE TABLE IF NOT EXISTS kontakter (
        email TEXT PRIMARY KEY,
        name TEXT,
        phone TEXT,
        last_purchase TEXT,
        events TEXT,
        synced_to_brevo INTEGER DEFAULT 0,
        created_at TEXT DEFAULT CURRENT_TIMESTAMP
    )
""")

# ====================== HJÄLPFUNKTIONER ======================
def hämta_tickster_orders():
    if not TICKSTER_API_KEY or not TICKSTER_BASE_URL:
        st.error("Tickster API-nyckel eller URL saknas. Lägg till i secrets.")
        return []
    
    # OBS: Ändra endpoint nedan när du får exakt URL från Tickster
    url = f"{TICKSTER_BASE_URL}/orders"   # ← ÄNDRA DENNA om Tickster ger en annan endpoint
    headers = {"Authorization": f"Bearer {TICKSTER_API_KEY}"}
    params = {"limit": 1000}  
    
    try:
        resp = requests.get(url, headers=headers, params=params, timeout=30)
        resp.raise_for_status()
        return resp.json()
    except Exception as e:
        st.error(f"Fel vid hämtning från Tickster: {e}")
        return []

def spara_till_db(orders):
    if not orders:
        return 0
    
    cursor = conn.cursor()
    antal = 0
    
    for order in orders:
        # Anpassa dessa nycklar efter hur Tickster returnerar datan
        buyer = order.get("buyer") or order.get("customer", {})
        email = buyer.get("email")
        if not email:
            continue
            
        cursor.execute("""
            INSERT OR REPLACE INTO kontakter 
            (email, name, phone, last_purchase, events, synced_to_brevo)
            VALUES (?, ?, ?, ?, ?, 0)
        """, (
            email,
            buyer.get("name") or buyer.get("full_name"),
            buyer.get("phone"),
            order.get("purchase_date") or order.get("created_at"),
            json.dumps(order.get("events", []) or order.get("items", []))
        ))
        antal += 1
    
    conn.commit()
    return antal

def synka_till_brevo():
    cursor = conn.cursor()
    cursor.execute("SELECT email, name, phone FROM kontakter WHERE synced_to_brevo = 0")
    rows = cursor.fetchall()
    
    if not rows:
        st.info("Alla kontakter är redan synkade till Brevo.")
        return 0
    
    url = "https://api.brevo.com/v3/contacts"
    headers = {
        "api-key": BREVO_API_KEY,
        "Content-Type": "application/json"
    }
    
    synkade = 0
    for email, name, phone in rows:
        try:
            data = {
                "email": email,
                "attributes": {
                    "FIRSTNAME": name or "",
                    "SMS": phone or ""   # Brevo använder SMS för telefon
                },
                "listIds": [int(BREVO_LIST_ID)] if BREVO_LIST_ID else []
            }
            response = requests.post(url, headers=headers, json=data)
            if response.status_code in [200, 201]:
                synkade += 1
        except:
            pass  # fortsätt även om en kontakt failar
    
    # Markera som synkade
    cursor.execute("UPDATE kontakter SET synced_to_brevo = 1")
    conn.commit()
    
    return synkade

# ====================== SIDAN ======================
tab1, tab2, tab3 = st.tabs(["🔄 Synka Tickster", "📋 Alla kontakter", "✉️ Skicka nyhetsbrev"])

with tab1:
    st.subheader("Hämta nya biljettköpare från Tickster")
    col1, col2 = st.columns([1, 1])
    
    with col1:
        if st.button("🔄 Synka nu från Tickster", type="primary", use_container_width=True):
            with st.spinner("Hämtar ordrar från Tickster..."):
                orders = hämta_tickster_orders()
                if orders:
                    antal_sparade = spara_till_db(orders)
                    st.success(f"✅ {antal_sparade} kontakter sparade i databasen")
                    
                    with st.spinner("Synkar till Brevo..."):
                        antal_synkade = synka_till_brevo()
                        st.success(f"✅ {antal_synkade} kontakter skickade till Brevo!")

with tab2:
    st.subheader("Alla kontakter")
    df = pd.read_sql_query("SELECT email, name, phone, last_purchase, synced_to_brevo FROM kontakter ORDER BY last_purchase DESC", conn)
    
    if len(df) > 0:
        st.dataframe(df, use_container_width=True, hide_index=True)
        st.download_button(
            label="📥 Ladda ner som CSV",
            data=df.to_csv(index=False),
            file_name="playa_news_kontakter.csv",
            mime="text/csv"
        )
    else:
        st.info("Inga kontakter än. Synka från Tickster först.")

with tab3:
    st.subheader("Skapa och skicka nyhetsbrev")
    st.success("✅ Dina kontakter finns nu i Brevo")
    
    st.markdown("""
    ### Så här gör du:
    1. Klicka på knappen nedan
    2. Skapa ett nytt kampanj i Brevo
    3. Välj din lista "Alla kunder" (eller den du vill skicka till)
    4. Använd den enkla drag & drop-editorn
    """)
    
    st.link_button(
        "➡️ Öppna Brevo och skapa nytt nyhetsbrev",
        "https://app.brevo.com/campaigns",
        use_container_width=True,
        type="primary"
    )

# Sidebar
with st.sidebar:
    st.header("🌴 Playa News")
    total_kontakter = len(pd.read_sql("SELECT email FROM kontakter", conn))
    st.metric("Totalt kontakter", total_kontakter)
    
    st.divider()
    st.caption("Enkel synk från Tickster till Brevo\nByggd för Playa • 2026")
